yaml_variable_scanner:
  yamlFilePath:
    - "test/case1/site.yaml"
  ignoreYamlFilePath:
  ignoreYamlKey:
    - repo.reg-exp$
  checkFilePath:
    - "test/case1/**/*.md"
  ignoreCheckFilePath:
    - "test/case1/**/3.md"
  ignoreCheckText:
    # --- 
    # xxx
    # ---
    - ^---([\s\S]*?)---$

    # {% comment %}
    #   xxx
    # {% endcomment %}
    - ^{%\s*comment\s*%}([\s\S]*?){%\s*endcomment\s*%}$
  