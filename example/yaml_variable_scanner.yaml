yaml_variable_scanner:
  yamlFilePath:
    - "test/*.yaml"
  ignoreYamlFilePath:
  ignoreYamlKey:
  checkFilePath:
    - "test/**/*.md"
  ignoreCheckFilePath:
    # - "test/content/**.md"
  ignoreCheckText:
    # --- 
    # xxx
    # ---
    - ^---([\s\S]*?)---$

    # {% comment %}
    #   xxx
    # {% endcomment %}
    - ^{%\s*comment\s*%}([\s\S]*?){%\s*endcomment\s*%}$
  